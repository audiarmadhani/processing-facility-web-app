# Dashboard source of truth

This application is maintained in a **separate repository**:

**https://github.com/audiarmadhani/processingfacility-dashboard**

Clone and deploy from that repo. Vercel should import `audiarmadhani/processingfacility-dashboard` with the repository root as the project root (not a subdirectory).

The copy under `processing-facility-web-app/dashboard/` is kept for local reference; prefer the dedicated repo for commits and deployments.
