# Driver app source of truth

This application is maintained in a **separate repository**:

**https://github.com/audiarmadhani/processing-facility-driver-app**

Clone and deploy from that repo. Vercel should import `audiarmadhani/processing-facility-driver-app` with the repository root as the project root.

The copy under `processing-facility-web-app/driver-app/` in the platform monorepo is for local reference; use the dedicated repo for commits and deployments.
